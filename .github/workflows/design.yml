name: Git Artwork SVG
on:
    schedule:
        - cron: '0 0 * * 0'
    workflow_dispatch:

jobs:
    build:
        name: Make gitartwork SVG
        runs-on: ubuntu-latest
        permissions: 
            contents: write
        steps:
            - uses: actions/checkout@v4
            - uses: jasineri/gitartwork@v1
              with:
                  user_name: toufikseu61 
                  text: TOUFIK
            - uses: jasineri/simple-push-action@v1
